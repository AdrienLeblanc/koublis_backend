spring:
    data:
        mongodb:
          host: ${MONGODB_SERVER:localhost}
          database: ${MONGODB_DATABASE:koublis}
          username: admin
          password: admin

koublis:
  security:
    jwtSecret: ${SECRET_KEY}
    jwtExpiration: 24h